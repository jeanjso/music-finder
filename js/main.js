import { searchMusic } from "./api.js";
import { renderResults } from "./ui.js";

// 🚧 DEBUG TEMPORAL — borrar antes de cerrar el Issue #4
window.searchMusic = searchMusic;
window.renderResults = renderResults;
